services:
  # 🎮 خدمة اللعبة (Web Service)
  - type: web
    name: sudoku-game-web
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production
      # أضف هذه يدوياً من لوحة Render:
      - key: GAME_URL
        sync: false
      - key: INTERNAL_API_KEY
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: DATABASE_PATH
        value: /opt/render/project/src/sudoku.db

  # 🤖 خدمة البوت (Worker Service)
  - type: worker
    name: sudoku-game-bot
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      # أضف هذه يدوياً من لوحة Render:
      - key: BOT_TOKEN
        sync: false
      - key: GAME_URL
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: INTERNAL_API_KEY
        sync: false
      - key: DATABASE_PATH
        value: /opt/render/project/src/sudoku.db